import styled from 'styled-components';

const ContactListWrapper = styled.section``;

const ContactList = () => {
	return <ContactListWrapper>ContactList</ContactListWrapper>;
};

export default ContactList;
